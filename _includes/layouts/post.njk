---
layout: layouts/base.njk
---

<article class="max-w-screen-md mx-auto space-y-6" role="article" aria-labelledby="{{ title | slug }}-title">
  <header>
    <h1 id="{{ title | slug }}-title">{{ title }}</h1>
  </header>

  {{ content | safe }}
</article>